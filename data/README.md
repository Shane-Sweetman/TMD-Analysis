# Data Policy

No large generated datasets are committed to this repository.

The public repository contains source code, documentation, thesis material, presentations, plotting scripts, and small result summaries. Large ROOT files and generated event samples are intentionally excluded because they are too large for a clean GitHub repository.

For serious reproduction, generate fresh PYTHIA samples with the scripts in `scripts/`, then store the resulting ROOT outputs outside the git tree.
